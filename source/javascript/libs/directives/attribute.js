export default {
    update(value) {
        this.node.setAttribute(this.attrName, value);
    }
};

// const attribute = {

//     update: ( value ) => {

//         this.node.setAttribute( this.attrName, value );

//     }

// };

// export default attribute;